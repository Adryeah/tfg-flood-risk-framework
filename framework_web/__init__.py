# Marker so `framework_web.backend.main:app` import path works both in
# Docker (PYTHONPATH=/app) and in local dev. The file is intentionally
# empty — Python only needs it to recognise the directory as a package.
